<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HHPhillyApp.EntryPage"
             Title= "Resources List">
  <ContentPage.Padding>
    <OnPlatform x:TypeArguments="Thickness" iOS="0, 20, 0, 0" />
  </ContentPage.Padding>
    <ListView x:Name="lstData" HasUnevenRows="false" Header="Header Value" ItemSelected="OnClicked" >
        <ListView.HeaderTemplate>
        <DataTemplate>
          <StackLayout Orientation="Horizontal" Padding="5,5,5,5">
          <Picker x:Name="picker" Title="Resource Categories" SelectedIndexChanged="OnPickerSelectedIndexChanged">
              <Picker.Items>
                 <x:String> All Resources</x:String>
                 <x:String> Men</x:String>
                 <x:String> Women</x:String>
              </Picker.Items>
          </Picker>
          </StackLayout>       
        </DataTemplate>
      </ListView.HeaderTemplate>
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Orientation="Horizontal" Padding="5,5,5,5">
                <Label Text="{Binding Name}" FontSize="Medium" />
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
</ContentPage>